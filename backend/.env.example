# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_NAME=sgipc

# Seguridad JWT (genera una clave larga y aleatoria)
JWT_SECRET=cambia_esto_por_una_clave_secreta_larga_y_aleatoria

# Servidor
PORT=8080

# CORS: URL del frontend (separar con coma si son varias)
CORS_ORIGIN=http://localhost:5173
